'use strict';

const keyMirror = require('react/lib/keyMirror');

module.exports = keyMirror({
  LOGIN: null,
  LOGIN_FAIL: null,
  LOGIN_SUCCESS: null,
  TOKEN: null,
  TOKEN_FAIL: null,
  TOKEN_SUCCESS: null,
});
